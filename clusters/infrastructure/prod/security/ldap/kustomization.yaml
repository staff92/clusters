apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: lldap
resources:
  - namespace.yaml
  - secret.yaml
  - repo.yaml
  - release.yaml
  - cronjob.yaml
configMapGenerator:
  - name: lldap-values
    files:
      - values.yaml
generatorOptions:
  disableNameSuffixHash: true