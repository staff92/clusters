apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: vault
resources:
  - namespace.yaml
  - repo.yaml
  - secret.yaml
  - release.yaml
configMapGenerator:
  - name: vault-values
    files:
      - values.yaml
generatorOptions:
  disableNameSuffixHash: true