apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: polaris
resources:
  - namespace.yaml
  - repo.yaml
  - release.yaml
configMapGenerator:
  - name: polaris-values
    files:
      - values.yaml
generatorOptions:
  disableNameSuffixHash: true