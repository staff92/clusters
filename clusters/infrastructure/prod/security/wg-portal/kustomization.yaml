apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: wg-portal
resources:
  - namespace.yaml
  - secret.yaml
  - repo.yaml
  - release.yaml
configMapGenerator:
  - name: wg-portal-values
    files:
      - values.yaml
generatorOptions:
  disableNameSuffixHash: true