apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: media
resources:
  - secret.yaml
  - repo.yaml
  - release.yaml
  - ingress.yaml
configMapGenerator:
  - name: tracearr-values
    files:
      - values.yaml
generatorOptions:
  disableNameSuffixHash: true