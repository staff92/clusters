apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sftp-data
  namespace: media
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: "openebs-hostpath"
  resources:
    requests:
      storage: 50Gi