apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: media
resources:
  - namespace.yaml
  - repo.yaml
  - pvc.yaml
  - release.yaml
configMapGenerator:
  - name: servarr-values
    files:
      - values.yaml
generatorOptions:
  disableNameSuffixHash: true