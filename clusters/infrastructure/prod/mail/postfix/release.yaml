---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: mail
  namespace: default
spec:
  interval: 5m
  chart:
    spec:
      chart: mail
      version: '5.1.0'
      sourceRef:
        kind: HelmRepository
        name: mail
        namespace: mail
  valuesFrom:
    - kind: ConfigMap
      name: mail-values
      valuesKey: values.yaml
  # targetNamespace: local-storage
  # createTargetNamespace: true 
  # install:
  #   createNamespace: true
      # namespace: default

      # valuesFiles:
      #   - ./clusters/infrastructure/podinfo/values-env.yaml
        #- values-env.yaml
        #- values-env.yaml
  # values:
  #   replicaCount: 1
  #   resource:
  #     requests:
  #       cpu: 100m
  #       memory: 64Mi




# ---
# apiVersion: source.toolkit.fluxcd.io/v1
# kind: HelmChart
# metadata:
#   name: podinfo
#   namespace: default
# spec:
#   interval: 5m0s
#   chart: podinfo
#   reconcileStrategy: ChartVersion
#   sourceRef:
#     kind: HelmRepository
#     name: podinfo
#   version: '5.*'