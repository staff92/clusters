apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mail-pvc
  namespace: mail
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: "openebs-hostpath"
  resources:
    requests:
      storage: 1Gi