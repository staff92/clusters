apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: immich
resources:
  - namespace.yaml
  - pvc.yaml
  - secret.yaml
  - repo.yaml
  - release.yaml
  - ingress.yaml
configMapGenerator:
  - name: immich-values
    files:
      - values.yaml
  - name: immich-db-values
    files:
      - immich-db-values.yaml
  - name: immich-operator-db-values
    files:
      - immich-operator-db-values.yaml
generatorOptions:
  disableNameSuffixHash: true