apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: umami
resources:
  - namespace.yaml
  - repo.yaml
  - pvc.yaml
  - release.yaml
configMapGenerator:
  - name: umami-values
    files:
      - values.yaml
generatorOptions:
  disableNameSuffixHash: true