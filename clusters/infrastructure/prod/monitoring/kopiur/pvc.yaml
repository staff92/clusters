apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: kopiur-repo-data
  namespace: kopiur
spec:
  storageClassName: openebs-hostpath
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 50Gi