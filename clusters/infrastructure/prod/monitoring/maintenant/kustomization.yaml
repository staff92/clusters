apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: maintenant
resources:
  - namespace.yaml
  - repo.yaml
  - release.yaml
configMapGenerator:
  - name: maintenant-values
    files:
      - values.yaml
generatorOptions:
  disableNameSuffixHash: true