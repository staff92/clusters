apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: vpa
resources:
  - namespace.yaml
  - repo.yaml
  - release.yaml
configMapGenerator:
  - name: vpa-values
    files:
      - values.yaml
generatorOptions:
  disableNameSuffixHash: true