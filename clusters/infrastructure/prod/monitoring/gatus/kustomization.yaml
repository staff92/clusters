apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: gatus
resources:
  - namespace.yaml
  - repo.yaml
  - release.yaml
  - secret.yaml
configMapGenerator:
  - name: gatus-values
    files:
      - values.yaml
generatorOptions:
  disableNameSuffixHash: true