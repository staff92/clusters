apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: alert
resources:
  - namespace.yaml
  - repo.yaml
  - release.yaml
  # - secret.yaml
configMapGenerator:
  - name: alert-values
    files:
      - values.yaml
generatorOptions:
  disableNameSuffixHash: true